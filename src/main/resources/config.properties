# 服务监听端口
server.port=58080
# API 鉴权 Token (请求时必须携带且匹配)
auth.token=ceroxe-secret-8888
# SSL 证书路径
ssl.cert.path=./cert/p.ceroxe.fun_bundle.crt
ssl.key.path=./cert/p.ceroxe.fun.key
# 订单等待超时时间 (秒)
order.timeout.seconds=60
# 回调重试
callback.retry.count=3
callback.retry.interval.ms=2000